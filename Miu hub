--// MIU HUB UPDATE SCREEN + AUTO KICK

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MIUHUB_UPDATE"

pcall(function()
	ScreenGui.Parent = game.CoreGui
end)

-- Main Frame
local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0, 350, 0, 180)
Main.Position = UDim2.new(0.5, -175, 0.5, -90)
Main.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 18)

-- Stroke
local Stroke = Instance.new("UIStroke")
Stroke.Parent = Main
Stroke.Color = Color3.fromRGB(255,255,255)
Stroke.Thickness = 2

-- Title
local Title = Instance.new("TextLabel")
Title.Parent = Main
Title.Size = UDim2.new(1,0,0,45)
Title.BackgroundTransparency = 1
Title.Text = "🌸 MIU HUB"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 30
Title.TextColor3 = Color3.new(1,1,1)

-- Status
local Status = Instance.new("TextLabel")
Status.Parent = Main
Status.Size = UDim2.new(1,-20,0,60)
Status.Position = UDim2.new(0,10,0,55)
Status.BackgroundTransparency = 1
Status.Text = "⚠️ MIU HUB đang update...\nVui lòng quay lại sau!"
Status.Font = Enum.Font.GothamSemibold
Status.TextSize = 20
Status.TextWrapped = true
Status.TextColor3 = Color3.new(1,1,1)

-- Countdown
local Countdown = Instance.new("TextLabel")
Countdown.Parent = Main
Countdown.Size = UDim2.new(1,0,0,30)
Countdown.Position = UDim2.new(0,0,1,-40)
Countdown.BackgroundTransparency = 1
Countdown.Font = Enum.Font.GothamBold
Countdown.TextSize = 18
Countdown.TextColor3 = Color3.fromRGB(255,255,200)

-- Countdown Loop
for i = 10,1,-1 do
	Countdown.Text = "⏳ Kick sau "..i.." giây..."
	task.wait(1)
end

-- Kick Player
LocalPlayer:Kick("MIU HUB đang update, vui lòng quay lại sau!")
